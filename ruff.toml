[lint]
select = ["F", "PLW0127"]  # pyflakes: unused imports and variables, undefined names, ...

[lint.per-file-ignores]
# Package initializers re-export names for the public API.
"**/__init__.py" = ["F401"]
